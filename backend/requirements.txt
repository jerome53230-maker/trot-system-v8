# Trot System v8.1 - Requirements
# Version améliorée avec corrections et optimisations
# Python 3.11+ (compatible Python 3.13)

# Framework Web
Flask==3.0.0
flask-cors==4.0.0
gunicorn==21.2.0

# HTTP Requests (scraping PMU avec retry)
requests==2.31.0

# Google Generative AI (analyse IA avec gestion quota)
google-generativeai==0.8.3

# Optional mais recommandé pour production
# python-dotenv==1.0.0  # Gestion variables d'environnement
# redis==5.0.1  # Cache distribué (à activer plus tard)
# psycopg2-binary==2.9.9  # PostgreSQL (à activer plus tard)
# sentry-sdk[flask]==1.40.0  # Monitoring erreurs (à activer plus tard)
